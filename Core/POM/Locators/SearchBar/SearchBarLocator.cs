using System;
using OpenQA.Selenium;

namespace Core.POM.Locators.SearchBarLocator
{
    public static class SearchBarLocator
    {
        internal static readonly By PriceSliderMax = By.CssSelector("div[class='slider - handle max - slider - handle round']");
    }
}
